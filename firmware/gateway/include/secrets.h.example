/**
 * secrets.h.example — copy to secrets.h and fill in real values.
 * secrets.h is git-ignored; never commit Wi-Fi credentials.
 */

#pragma once

// Wi-Fi credentials for forwarding packets to backend
#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"

// Backend API endpoint
#define API_BASE_URL  "http://192.168.1.x:8000"
